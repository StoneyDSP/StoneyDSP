# Claude Project Instructions

Read `AGENTS.md` first. It is the canonical repository guidance and repo-local
skill index.

If `.agents/current-work/00-index.md` exists locally, read it and the matching
private handoff note for the active task. Treat that state as non-canonical
working context between the user and current agent.

Do not duplicate project rules or skill lists here; keep `AGENTS.md`,
`.agents/project-context.md`, and the repo-local skills canonical.
